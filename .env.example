# --- Public (safe for browser) ---
NEXT_PUBLIC_SUPABASE_URL=https://drcahfhippwhiiqwjept.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_igO5LgJBBL15b9eagSjfSA_Nw3-PWU1

# --- Server-only: NEVER prefix with NEXT_PUBLIC_, never import into client components ---
# Direct Postgres connection, used only by migration tooling / server-side scripts.
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.drcahfhippwhiiqwjept.supabase.co:5432/postgres

# Service-role key (bypasses RLS). Required only for trusted server-side admin
# operations (e.g. staff invitation, storage housekeeping). NEVER expose to the client
# and never import it into any file under app/ that could be bundled client-side.
SUPABASE_SERVICE_ROLE_KEY=

# Add payment provider secrets (e.g. M-Pesa Daraja API credentials) here once configured.
# MPESA_CONSUMER_KEY=
# MPESA_CONSUMER_SECRET=
# MPESA_PASSKEY=
